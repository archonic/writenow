<% content_for :title, "Editing #{@document.name}" %>
<% content_for :left_nav do %>
  <%= render RubyUI::Link.new(variant: :secondary, href: document_path(@document)) do %>
    <i class="ri-arrow-left-line"></i>
    Document
  <% end %>
<% end %>
<% content_for :right_nav do %>
  <%= render Documents::SettingsDrawer.new(model: @document) %>
<% end %>

<%= render Documents::Edit.new(model: @document) %>

